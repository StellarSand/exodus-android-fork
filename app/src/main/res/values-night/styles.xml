<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Inherit base theme to allow overriding Dark Theme as per API levels -->
    <style name="Theme.Exodus.Dark" parent="BaseTheme">
        <item name="chipIconTint">@color/textColorLikeWhite</item>
        <item name="android:textColor">@color/textColorLikeWhite</item>
        <item name="android:textColorLink">@color/textColorLikeWhite</item>
        <item name="elevationOverlayEnabled">false</item>
    </style>

    <style name="Theme.Exodus" parent="Theme.Exodus.Dark" />

</resources>
